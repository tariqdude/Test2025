---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import SystemHighlights from '../components/SystemHighlights.astro';
import ModernCard from '../components/ui/ModernCard.astro';
import ModernButton from '../components/ui/ModernButton.astro';
import { withBasePath } from '../utils/helpers';

const pillars = [
  {
    title: 'Opinionated by default',
    description:
      'We trimmed the starter down to Astro, Tailwind, and content collections so every build works on static hosting without additional services.',
  },
  {
    title: 'Optimized for GitHub Pages',
    description:
      'Base paths, asset handling, and prerendered routes are set up for repository pages. Deploy straight from main and know the site will publish.',
  },
  {
    title: 'Documented patterns',
    description:
      'Each section uses plain Astro components and utility classes so you can scan a file, change copy, and ship updates quickly.',
  },
];

const timeline = [
  {
    year: '2023',
    heading: 'Prototype',
    copy: 'Created the original multi-framework playground to evaluate Astro islands and real-time charts.',
  },
  {
    year: '2024',
    heading: 'Reality check',
    copy: 'Discovered GitHub Pages limitations and the maintenance overhead introduced by several client runtimes.',
  },
  {
    year: '2025',
    heading: 'Simplified starter',
    copy: 'Rebuilt the template around static content and pragmatic sections so teams can publish status updates without friction.',
  },
];

const highlightItems = pillars.map((pillar, index) => ({
  label: String(index + 1).padStart(2, '0'),
  title: pillar.title,
  description: pillar.description,
}));

const principles = [
  {
    name: 'Calm surfaces',
    detail:
      'We limit motion, color, and noise so stakeholders can read status updates without burning cycles parsing layout changes.',
  },
  {
    name: 'Predictable delivery',
    detail:
      'Every command in the README is battle-tested against GitHub Pages. No hidden scripts, no server toggles.',
  },
  {
    name: 'Maintainer empathy',
    detail:
      'We document trade-offs and ship CLI helpers so product owners and engineers can extend the system with confidence.',
  },
];
---

<MarketingLayout
  title="About | Elite Project"
  description="Learn why this Astro starter prioritises reliability and static-friendly patterns."
>
  <section class="relative overflow-hidden bg-zinc-950">
    <div
      class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]"
    >
    </div>
    <div
      class="relative mx-auto flex max-w-6xl flex-col gap-12 px-6 pb-24 pt-28 lg:flex-row lg:items-center"
    >
      <div class="max-w-xl">
        <div
          class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-zinc-900 border border-zinc-800 mb-6"
        >
          <span class="w-2 h-2 rounded-full bg-accent-500 animate-pulse"></span>
          <span class="text-xs font-mono text-zinc-400 tracking-wider"
            >MISSION_STATEMENT</span
          >
        </div>
        <h1
          class="mt-6 text-4xl font-display font-bold text-white sm:text-5xl lg:text-6xl uppercase tracking-tight"
        >
          Shipping faster by <span class="text-accent-500">cutting</span> every unnecessary
          dependency.
        </h1>
        <p class="mt-6 text-lg text-zinc-400 font-mono leading-relaxed">
          // Elite Project began as a sandbox for flashy UI experiments. The
          2025 rewrite moves in the opposite direction: a calm foundation that
          keeps Astro sites light, maintainable, and ready for GitHub Pages.
        </p>
        <div class="mt-10 flex flex-wrap gap-4">
          <ModernButton
            href={withBasePath('services/')}
            variant="primary"
            className="font-mono uppercase tracking-wider"
          >
            Review_Services
          </ModernButton>
          <ModernButton
            href={withBasePath('contact/')}
            variant="secondary"
            className="font-mono uppercase tracking-wider"
          >
            Meet_Maintainers
          </ModernButton>
        </div>
      </div>
      <ModernCard
        variant="minimal"
        className="grid flex-1 gap-4 p-6 lg:max-w-md relative"
      >
        <div class="absolute -top-1 -left-1 w-3 h-3 bg-accent-500"></div>
        <div class="absolute -bottom-1 -right-1 w-3 h-3 bg-zinc-700"></div>

        <div
          class="flex items-baseline justify-between text-white border-b border-zinc-800 pb-4 mb-2"
        >
          <p class="text-xs font-mono uppercase tracking-wider text-zinc-500">
            VERSION_CADENCE
          </p>
          <span class="text-sm font-mono font-bold text-accent-500"
            >2024 {'->'} 2025</span
          >
        </div>
        <div class="grid grid-cols-2 gap-4">
          <ModernCard
            variant="minimal"
            className="bg-zinc-950 p-5 hover:border-accent-500 transition-colors group"
          >
            <p
              class="text-3xl font-display font-bold text-white group-hover:text-accent-500 transition-colors"
            >
              4×
            </p>
            <p class="mt-2 text-xs text-zinc-500 font-mono uppercase">
              Reduced_Dependencies
            </p>
          </ModernCard>
          <ModernCard
            variant="minimal"
            className="bg-zinc-950 p-5 hover:border-accent-500 transition-colors group"
          >
            <p
              class="text-3xl font-display font-bold text-white group-hover:text-accent-500 transition-colors"
            >
              100%
            </p>
            <p class="mt-2 text-xs text-zinc-500 font-mono uppercase">
              Static_Safe_Routes
            </p>
          </ModernCard>
        </div>
        <ModernCard
          variant="minimal"
          className="bg-zinc-950 p-5 hover:border-accent-500 transition-colors"
        >
          <p
            class="text-sm font-mono font-bold text-white uppercase tracking-wider"
          >
            DEPLOYMENT_PROOF
          </p>
          <p class="mt-2 text-xs text-zinc-400 font-mono">
            // Validated with clean type checks, reproducible builds, and GitHub
            Pages-friendly asset paths.
          </p>
        </ModernCard>
      </ModernCard>
    </div>
  </section>

  <section class="border-y border-zinc-800 bg-zinc-900/50">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <SystemHighlights
        eyebrow="SYSTEM_GUIDELINES"
        title="Three pillars keep the starter pragmatic"
        intro="// Everything in this repository is there for a reason: faster delivery, lower runtime risk, and clearer documentation for stakeholders."
        items={highlightItems}
      />
    </div>
  </section>

  <section class="bg-zinc-950 relative overflow-hidden">
    <div
      class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]"
    >
    </div>
    <div class="mx-auto max-w-5xl px-6 py-20 relative">
      <div class="grid gap-12 lg:grid-cols-[1.1fr_0.9fr] lg:items-start">
        <div>
          <p
            class="text-xs font-mono font-bold uppercase tracking-widest text-accent-500"
          >
            OPERATING_PRINCIPLES
          </p>
          <h2 class="mt-4 text-3xl font-display font-bold text-white">
            Why we invest in calm delivery surfaces
          </h2>
          <p class="mt-6 text-base text-zinc-400 font-mono leading-relaxed">
            // The best documentation sites look uneventful. We remove
            distractions so leadership and engineers can review status updates
            and service playbooks without fighting UI flourishes. Every decision
            below feeds that outcome.
          </p>
        </div>
        <div class="grid gap-6">
          {
            principles.map(principle => (
              <ModernCard
                variant="minimal"
                className="p-6 hover:border-accent-500 transition-colors group relative"
              >
                <div class="absolute top-0 right-0 w-3 h-3 border-t border-r border-zinc-700 group-hover:border-accent-500 transition-colors" />
                <div class="absolute bottom-0 left-0 w-3 h-3 border-b border-l border-zinc-700 group-hover:border-accent-500 transition-colors" />

                <h3 class="text-lg font-mono font-bold text-white group-hover:text-accent-500 transition-colors uppercase tracking-wider">
                  {principle.name}
                </h3>
                <p class="mt-3 text-sm leading-relaxed text-zinc-400 font-mono">
                  {principle.detail}
                </p>
              </ModernCard>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <section class="bg-zinc-900 border-y border-zinc-800">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <div
        class="flex flex-col gap-10 lg:flex-row lg:items-start lg:justify-between"
      >
        <div class="max-w-xl">
          <p
            class="text-xs font-mono font-bold uppercase tracking-widest text-zinc-500"
          >
            MILESTONES
          </p>
          <h2 class="mt-4 text-3xl font-display font-bold text-white">
            How we arrived here
          </h2>
          <p class="mt-4 text-base text-zinc-400 font-mono">
            // We integrated every lesson from past deployments into a
            repeatable starter. The timeline shows where we trimmed noise and
            doubled down on static clarity.
          </p>
        </div>
        <div class="flex-1">
          <div class="relative border-l border-zinc-800 pl-8 ml-4">
            <div class="space-y-12">
              {
                timeline.map(entry => (
                  <div class="relative group">
                    <div class="absolute -left-[2.4rem] top-1 h-4 w-4 border border-zinc-600 bg-zinc-900 group-hover:border-accent-500 group-hover:bg-accent-500 transition-colors" />
                    <p class="text-sm font-mono font-bold text-accent-500">
                      [{entry.year}]
                    </p>
                    <h3 class="mt-2 text-xl font-display font-bold text-white group-hover:text-accent-500 transition-colors">
                      {entry.heading}
                    </h3>
                    <p class="mt-2 text-sm text-zinc-400 font-mono border-l-2 border-zinc-800 pl-4 group-hover:border-accent-500 transition-colors">
                      {entry.copy}
                    </p>
                  </div>
                ))
              }
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-zinc-950 relative overflow-hidden">
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_center,_rgba(249,115,22,0.05),_transparent_70%)]"
    >
    </div>
    <div class="mx-auto max-w-5xl px-6 py-20 relative">
      <ModernCard
        variant="minimal"
        className="bg-zinc-900/80 px-8 py-14 text-center text-zinc-50 backdrop-blur-sm relative"
      >
        <div
          class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-accent-500 to-transparent opacity-50"
        >
        </div>

        <p
          class="text-xs font-mono font-bold uppercase tracking-widest text-accent-500"
        >
          NEXT_STEPS
        </p>
        <h2 class="mt-6 text-3xl font-display font-bold text-white">
          Ready to extend the system to your operations?
        </h2>
        <p class="mt-5 text-base text-zinc-400 font-mono max-w-2xl mx-auto">
          // Bring your own content strategy and we will translate it into
          static-first sections, CLI helpers, and playbooks that teams can
          maintain long after launch.
        </p>
        <div class="mt-8 flex flex-wrap justify-center gap-3">
          <ModernButton
            href={withBasePath('contact/')}
            variant="primary"
            className="font-mono uppercase tracking-wider"
          >
            Schedule_Discovery
          </ModernButton>
          <ModernButton
            href={withBasePath('services/')}
            variant="secondary"
            className="font-mono uppercase tracking-wider"
          >
            View_Catalogue
          </ModernButton>
        </div>
      </ModernCard>
    </div>
  </section>
</MarketingLayout>
