---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import SystemHighlights from '../components/SystemHighlights.astro';
import { withBasePath } from '../utils/helpers';

const pillars = [
  {
    title: 'Opinionated by default',
    description:
      'We trimmed the starter down to Astro, Tailwind, and content collections so every build works on static hosting without additional services.',
  },
  {
    title: 'Optimized for GitHub Pages',
    description:
      'Base paths, asset handling, and prerendered routes are set up for repository pages. Deploy straight from main and know the site will publish.',
  },
  {
    title: 'Documented patterns',
    description:
      'Each section uses plain Astro components and utility classes so you can scan a file, change copy, and ship updates quickly.',
  },
];

const timeline = [
  {
    year: '2023',
    heading: 'Prototype',
    copy: 'Created the original multi-framework playground to evaluate Astro islands and real-time charts.',
  },
  {
    year: '2024',
    heading: 'Reality check',
    copy: 'Discovered GitHub Pages limitations and the maintenance overhead introduced by several client runtimes.',
  },
  {
    year: '2025',
    heading: 'Simplified starter',
    copy: 'Rebuilt the template around static content and pragmatic sections so teams can publish status updates without friction.',
  },
];

const highlightItems = pillars.map((pillar, index) => ({
  label: String(index + 1).padStart(2, '0'),
  title: pillar.title,
  description: pillar.description,
}));

const principles = [
  {
    name: 'Calm surfaces',
    detail:
      'We limit motion, color, and noise so stakeholders can read status updates without burning cycles parsing layout changes.',
  },
  {
    name: 'Predictable delivery',
    detail:
      'Every command in the README is battle-tested against GitHub Pages. No hidden scripts, no server toggles.',
  },
  {
    name: 'Maintainer empathy',
    detail:
      'We document trade-offs and ship CLI helpers so product owners and engineers can extend the system with confidence.',
  },
];
---

<MarketingLayout
  title="About | Elite Project"
  description="Learn why this Astro starter prioritises reliability and static-friendly patterns."
>
  <section class="relative overflow-hidden bg-neutral-950">
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(80,80,80,0.45),_transparent_60%)]"
    >
    </div>
    <div
      class="relative mx-auto flex max-w-6xl flex-col gap-12 px-6 pb-24 pt-28 lg:flex-row lg:items-center"
    >
      <div class="max-w-xl">
        <p
          class="text-xs font-semibold uppercase tracking-[0.35em] text-neutral-400"
        >
          Our mission
        </p>
        <h1
          class="mt-6 text-4xl font-semibold text-white sm:text-5xl lg:text-6xl"
        >
          Shipping faster by cutting every unnecessary dependency.
        </h1>
        <p class="mt-6 text-lg text-neutral-300">
          Elite Project began as a sandbox for flashy UI experiments. The 2025
          rewrite moves in the opposite direction: a calm foundation that keeps
          Astro sites light, maintainable, and ready for GitHub Pages.
        </p>
        <div class="mt-10 flex flex-wrap gap-4">
          <a
            href={withBasePath('services/')}
            class="inline-flex items-center justify-center rounded-full bg-brand-600 px-5 py-2 text-sm font-semibold text-white transition hover:bg-brand-700"
          >
            Review our services
          </a>
          <a
            href={withBasePath('contact/')}
            class="inline-flex items-center justify-center rounded-full border border-white/20 px-5 py-2 text-sm font-semibold text-white transition hover:border-white/40"
          >
            Meet the maintainers
          </a>
        </div>
      </div>
      <div
        class="grid flex-1 gap-4 rounded-3xl border border-white/10 bg-neutral-900/60 p-6 backdrop-blur lg:max-w-md"
      >
        <div class="flex items-baseline justify-between text-white">
          <p class="text-xs uppercase tracking-[0.25em] text-neutral-400">
            Version cadence
          </p>
          <span class="text-sm font-semibold text-neutral-300">2024 → 2025</span
          >
        </div>
        <div class="grid grid-cols-2 gap-4">
          <div class="rounded-2xl border border-white/10 bg-neutral-950/40 p-5">
            <p class="text-3xl font-semibold text-white">4×</p>
            <p class="mt-2 text-xs text-neutral-400">
              Fewer dependencies vs early prototype
            </p>
          </div>
          <div class="rounded-2xl border border-white/10 bg-neutral-950/40 p-5">
            <p class="text-3xl font-semibold text-white">100%</p>
            <p class="mt-2 text-xs text-neutral-400">
              Static-safe routes across every page
            </p>
          </div>
        </div>
        <div class="rounded-2xl border border-white/10 bg-neutral-950/40 p-5">
          <p class="text-sm font-semibold text-white">
            Deployment proof points
          </p>
          <p class="mt-2 text-xs text-neutral-400">
            Validated with clean type checks, reproducible builds, and GitHub
            Pages-friendly asset paths.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="border-y border-neutral-200 bg-neutral-50">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <SystemHighlights
        eyebrow="What guides the system"
        title="Three pillars keep the starter pragmatic"
        intro="Everything in this repository is there for a reason: faster delivery, lower runtime risk, and clearer documentation for stakeholders."
        items={highlightItems}
      />
    </div>
  </section>

  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-20">
      <div class="grid gap-12 lg:grid-cols-[1.1fr_0.9fr] lg:items-start">
        <div>
          <p
            class="text-xs font-semibold uppercase tracking-[0.3em] text-brand-600"
          >
            Operating principles
          </p>
          <h2 class="mt-4 text-3xl font-semibold text-neutral-900">
            Why we invest in calm delivery surfaces
          </h2>
          <p class="mt-6 text-base text-neutral-600">
            The best documentation sites look uneventful. We remove distractions
            so leadership and engineers can review status updates and service
            playbooks without fighting UI flourishes. Every decision below feeds
            that outcome.
          </p>
        </div>
        <div class="grid gap-6">
          {
            principles.map(principle => (
              <article class="rounded-2xl border border-neutral-200 bg-neutral-50 p-6">
                <h3 class="text-lg font-semibold text-neutral-900">
                  {principle.name}
                </h3>
                <p class="mt-3 text-sm leading-relaxed text-neutral-600">
                  {principle.detail}
                </p>
              </article>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  <section class="bg-neutral-950 text-neutral-50">
    <div class="mx-auto max-w-6xl px-6 py-20">
      <div
        class="flex flex-col gap-10 lg:flex-row lg:items-start lg:justify-between"
      >
        <div class="max-w-xl">
          <p
            class="text-xs font-semibold uppercase tracking-[0.35em] text-neutral-400"
          >
            Milestones
          </p>
          <h2 class="mt-4 text-3xl font-semibold text-white">
            How we arrived here
          </h2>
          <p class="mt-4 text-base text-neutral-300">
            We integrated every lesson from past deployments into a repeatable
            starter. The timeline shows where we trimmed noise and doubled down
            on static clarity.
          </p>
        </div>
        <div class="flex-1">
          <div class="relative border-l border-neutral-700 pl-8">
            <div
              class="absolute inset-y-0 -left-0.5 w-1 bg-gradient-to-b from-brand-500/60 via-brand-600 to-brand-700/60"
              aria-hidden="true"
            >
            </div>
            <div class="space-y-9">
              {
                timeline.map(entry => (
                  <div class="relative">
                    <div class="absolute -left-[1.25rem] top-2 h-3 w-3 rounded-full border border-neutral-950 bg-brand-500" />
                    <p class="text-sm font-semibold text-brand-400">
                      {entry.year}
                    </p>
                    <h3 class="mt-2 text-xl font-semibold text-white">
                      {entry.heading}
                    </h3>
                    <p class="mt-2 text-sm text-neutral-300">{entry.copy}</p>
                  </div>
                ))
              }
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-white">
    <div class="mx-auto max-w-5xl px-6 py-20">
      <div
        class="rounded-3xl border border-neutral-200 bg-neutral-950/95 px-8 py-14 text-center text-neutral-50 shadow-[0_45px_90px_-45px_rgba(17,17,17,0.45)]"
      >
        <p
          class="text-xs font-semibold uppercase tracking-[0.35em] text-brand-400"
        >
          Next steps
        </p>
        <h2 class="mt-6 text-3xl font-semibold text-white">
          Ready to extend the system to your operations?
        </h2>
        <p class="mt-5 text-base text-neutral-300">
          Bring your own content strategy and we will translate it into
          static-first sections, CLI helpers, and playbooks that teams can
          maintain long after launch.
        </p>
        <div class="mt-8 flex flex-wrap justify-center gap-3">
          <a
            class="inline-flex items-center justify-center rounded-full bg-brand-500 px-5 py-2 text-sm font-semibold text-white transition hover:bg-brand-600"
            href={withBasePath('contact/')}
          >
            Schedule a discovery call
          </a>
          <a
            class="inline-flex items-center justify-center rounded-full border border-white/20 px-5 py-2 text-sm font-semibold text-white transition hover:border-white/40"
            href={withBasePath('services/')}
          >
            View service catalogue
          </a>
        </div>
      </div>
    </div>
  </section>
</MarketingLayout>
