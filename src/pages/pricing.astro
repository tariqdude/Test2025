---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import ModernCard from '../components/ui/ModernCard.astro';
import ModernButton from '../components/ui/ModernButton.astro';
import { resolveHref, withBasePath } from '../utils/helpers';

const tiers = [
  {
    name: 'Starter',
    price: '$0',
    description: 'Clone the repo, follow the README, and deploy manually.',
    perks: [
      'Fully static Astro + Tailwind build configured for GitHub Pages.',
      'Sample content collections and RSS feed.',
      'MIT license for personal and commercial use.',
    ],
    cta: 'Get the template',
    href: 'https://github.com/tariqdude/Github-Pages-Project-v1',
  },
  {
    name: 'Team Launch',
    price: '$1,200',
    description: 'One-week engagement to tailor the template to your org.',
    perks: [
      'Brand styling updates and content migration assistance.',
      'Automation scripts for lint, test, and GitHub Pages deploy.',
      'Live working session to review maintenance workflows.',
    ],
    cta: 'Book a kickoff call',
    href: '/contact',
    highlight: true,
  },
  {
    name: 'Retainer',
    price: 'Custom',
    description: 'Ongoing support for releases, audits, and new sections.',
    perks: [
      'Quarterly accessibility, performance, and security reviews.',
      'Copywriting and content strategy assistance for updates.',
      'Priority response from the maintainers.',
    ],
    cta: 'Start a conversation',
    href: '/contact',
  },
];

const faqs = [
  {
    question: 'Do you offer payment schedules?',
    answer:
      'Yes. For Team Launch and Retainer packages we split payments into kickoff and handover invoices, with optional monthly plans for retainers.',
  },
  {
    question: 'Can we start from an existing Astro project?',
    answer:
      'Absolutely. We audit your current repository, identify blockers to static deployment, and adapt this system to fit without rewriting every page.',
  },
  {
    question: 'What happens after handover?',
    answer:
      'You keep the documentation, scripts, and workshop recordings. Retainers add quarterly reviews and priority support from the maintainers.',
  },
];
---

<MarketingLayout
  title="Pricing | Elite Project"
  description="Transparent pricing for adapting and maintaining the Elite Project starter."
>
  <section
    class="relative overflow-hidden bg-zinc-950 text-zinc-50 border-b border-zinc-800"
  >
    <div
      class="absolute inset-0 bg-[linear-gradient(to_right,#18181b_1px,transparent_1px),linear-gradient(to_bottom,#18181b_1px,transparent_1px)] bg-[size:4rem_4rem] [mask-image:radial-gradient(ellipse_60%_50%_at_50%_0%,#000_70%,transparent_100%)] opacity-20"
    >
    </div>
    <div class="relative mx-auto max-w-5xl px-6 pb-24 pt-28 text-center">
      <p
        class="text-xs font-bold uppercase tracking-[0.35em] text-accent-500 font-mono"
      >
        Pricing
      </p>
      <h1
        class="mt-6 text-4xl font-bold sm:text-5xl font-display uppercase tracking-tight"
      >
        Choose the level of support that keeps you shipping.
      </h1>
      <p class="mt-6 mx-auto max-w-3xl text-lg text-zinc-400 font-mono">
        Whether you want to self-host or rely on our team for ongoing
        improvements, every tier keeps the Astro site simple, reliable, and well
        documented.
      </p>
      <ModernCard
        variant="minimal"
        className="mt-10 inline-flex items-center gap-6 bg-zinc-900/60 px-6 py-3 text-sm text-zinc-300 font-mono"
      >
        <span class="inline-flex h-2 w-2 bg-accent-500 animate-pulse"></span>
        <span
          >All paid tiers include documentation, training, and priority inbox
          access.</span
        >
      </ModernCard>
    </div>
  </section>

  <section class="bg-zinc-900">
    <div class="mx-auto grid max-w-6xl gap-8 px-6 py-20 lg:grid-cols-3">
      {
        tiers.map((tier, index) => (
          <ModernCard
            variant="minimal"
            className={`flex h-full flex-col gap-6 p-8 animate-fade-in-up ${tier.highlight ? 'border-accent-500 bg-zinc-950' : 'bg-zinc-950/50'}`}
            style={`animation-delay: ${index * 100}ms; opacity: 0;`}
          >
            <div class="space-y-3">
              <span
                class={`inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-semibold uppercase tracking-[0.25em] ${tier.highlight ? 'bg-accent-500/20 text-accent-200' : 'bg-zinc-800 text-zinc-400'}`}
              >
                {tier.highlight ? 'Recommended' : 'Package'}
              </span>
              <h2 class="text-3xl font-semibold text-white">{tier.name}</h2>
              <p class="text-4xl font-semibold text-white">{tier.price}</p>
              <p class="text-sm text-zinc-400">{tier.description}</p>
            </div>
            <ul class="space-y-3 text-sm text-zinc-400">
              {tier.perks.map(perk => (
                <li class="flex items-start gap-2">
                  <span
                    class={`mt-1 inline-flex h-2 w-2 rounded-full ${tier.highlight ? 'bg-accent-400' : 'bg-accent-500'}`}
                  />
                  <span>{perk}</span>
                </li>
              ))}
            </ul>
            <div class="mt-auto">
              <ModernButton
                className="w-full"
                variant={tier.highlight ? 'primary' : 'secondary'}
                href={resolveHref(tier.href)}
              >
                {tier.cta}
              </ModernButton>
            </div>
          </ModernCard>
        ))
      }
    </div>
  </section>

  <section class="border-y border-zinc-800 bg-zinc-950">
    <div class="mx-auto max-w-5xl px-6 py-20">
      <div class="grid gap-12 lg:grid-cols-[1.2fr_0.8fr] lg:items-center">
        <div>
          <p
            class="text-xs font-semibold uppercase tracking-[0.3em] text-accent-500"
          >
            Not sure where to start?
          </p>
          <h2 class="mt-4 text-3xl font-semibold text-white">
            We tailor every engagement scope before you commit.
          </h2>
          <p class="mt-6 text-base text-zinc-400">
            Send a short brief or link to your existing site. We will suggest
            the tier that best balances your requirements, infrastructure, and
            timeline.
          </p>
          <div class="mt-8 flex flex-wrap gap-4">
            <ModernButton variant="primary" href={withBasePath('contact/')}>
              Talk with the team
            </ModernButton>
            <ModernButton variant="secondary" href={withBasePath('services/')}>
              Review services
            </ModernButton>
          </div>
        </div>
        <ModernCard
          variant="minimal"
          className="p-6 shadow-[0_40px_80px_-60px_rgba(17,17,17,0.25)]"
        >
          <h3 class="text-lg font-semibold text-white">Every plan includes</h3>
          <ul class="mt-5 space-y-3 text-sm text-zinc-400">
            <li class="flex items-start gap-2">
              <span class="mt-1 inline-flex h-2 w-2 rounded-full bg-accent-500"
              ></span>
              <span
                >Documentation bundle covering setup, content publishing, and
                maintenance routines.</span
              >
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-1 inline-flex h-2 w-2 rounded-full bg-accent-500"
              ></span>
              <span>Recorded walkthrough and Q&A session with your team.</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-1 inline-flex h-2 w-2 rounded-full bg-accent-500"
              ></span>
              <span>30 days of follow-up support for clarifying questions.</span
              >
            </li>
          </ul>
        </ModernCard>
      </div>
    </div>
  </section>

  <section class="border-t border-zinc-800 bg-zinc-900/30 py-24">
    <div class="mx-auto max-w-4xl px-6">
      <h2
        class="text-3xl font-bold text-center text-white font-display uppercase tracking-tight mb-16"
      >
        Frequently Asked Questions
      </h2>
      <div class="grid gap-8">
        {
          faqs.map(faq => (
            <ModernCard
              variant="minimal"
              className="bg-zinc-950 p-8 hover:border-zinc-700 transition-colors"
            >
              <h3 class="text-lg font-bold text-white font-display uppercase tracking-wide mb-4">
                {faq.question}
              </h3>
              <p class="text-zinc-400 font-mono leading-relaxed">
                {faq.answer}
              </p>
            </ModernCard>
          ))
        }
      </div>
    </div>
  </section>
</MarketingLayout>
